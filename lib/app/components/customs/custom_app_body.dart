import 'package:flutter/widgets.dart';

class CustomAppBody extends StatelessWidget {
  const CustomAppBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
